// Here is where we define our model for a user. Because viewers and streamers
// are both Twitch users, I think we should have a common model for both of
// them. The only difference is that the queue will belong to 1 user, which
// will make that user special and allow them admin access.
